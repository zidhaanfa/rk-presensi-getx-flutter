import 'package:flutter/material.dart';

class ApprovalRequestStatus extends StatelessWidget {
  const ApprovalRequestStatus({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child: Text('Approval Request Status'),
        ),
      ],
    );
  }
}
