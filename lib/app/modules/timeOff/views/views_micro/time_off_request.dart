import 'package:flutter/material.dart';

class TimeOffRequest extends StatelessWidget {
  const TimeOffRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(
          child: Text('Time Off Request'),
        ),
      ],
    );
  }
}
